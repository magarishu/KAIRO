import { Pool } from 'pg'; import { PrismaPg } from '@prisma/adapter-pg'; import { PrismaClient } from '@prisma/client'; import { config } from 'dotenv'; config(); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); const adapter = new PrismaPg(pool); const prisma = new PrismaClient({ adapter }); async function main() { await prisma.slaveAccount.deleteMany({}); const g = await prisma.group.deleteMany({}); console.log('Deleted', g.count, 'groups.'); } main().catch(console.error).finally(() => prisma.$disconnect());
